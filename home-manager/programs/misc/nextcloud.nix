#Nextcloud is a open source server software. It's the standard for private uses and hightly good for pentesting.
{
  services.nextcloud-client = {
    enable = true;
    #  startInBackground = true;
  };
}
